[pytest]
# CIRCE Intel Desk — configuração do pytest
# Restringe a coleta de testes à pasta tests/ para evitar que scripts
# utilitários em scripts/ (ex: test_ocr.py) sejam coletados como testes.
testpaths = tests
